# Clerk — publishable key only (same application as backend CLERK_PUBLISHABLE_KEY)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_from_clerk_dashboard

# API base (Vite dev server proxies /api to the backend when unset)
# VITE_API_URL=/api/v1
